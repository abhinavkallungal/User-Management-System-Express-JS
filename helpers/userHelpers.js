const db= require('../config/connection');
const collections= require('../config/collections')

module.exports={
    checkEmail:()=>{

    },
    signUp:(data)=>{
        console.log(data);
        
    }
}